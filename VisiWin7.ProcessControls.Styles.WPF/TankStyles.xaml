<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:pc="http://inosoft.com/ProcessControls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/VisiWin7.ProcessControls.Styles.WPF;component/ProcessControlStyles.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--StandardTank-Style-->
    <Style TargetType="pc:Tank" BasedOn="{StaticResource {x:Type pc:DefaultProcessControlBase}}">
        <Setter Property="Padding" Value="0"/>
        <Style.Resources>
            <DataTemplate x:Key="StyleTemplate">
                <Grid>
                    <Path StrokeThickness="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource StrokeThicknessFallback}}"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource ProcessControlStrokeBrush}}"
                          Stretch="Fill" 
                          Panel.ZIndex="1">
                        <Path.Data>
                            <PathGeometry Figures="m 0,0 l 10,0 l 0,10 l -10,0 z"/>
                        </Path.Data>
                    </Path>
                    <Rectangle VerticalAlignment="Bottom"
                               Fill="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource ProcessControlForegroundBrush}}"
                               Height="{Binding RectangleHeight, RelativeSource={RelativeSource AncestorType={x:Type pc:Tank}}, FallbackValue={StaticResource RectangleHeightFallback}}"/>
                </Grid>
            </DataTemplate>
        </Style.Resources>
    </Style>

    <!--RoundTank-Style-->
    <Style TargetType="pc:RoundTank" BasedOn="{StaticResource {x:Type pc:DefaultProcessControlBase}}">
        <Setter Property="Padding" Value="1"/>
        <Style.Resources>
            <DataTemplate x:Key="StyleTemplate">
                <Grid>
                    <Border Background="White">
                        <Border.OpacityMask>
                            <VisualBrush Stretch="Fill">
                                <VisualBrush.Visual>
                                    <Path Fill="White" Data="{Binding ElementName=GeoToBind}"/>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Border.OpacityMask>
                        <Rectangle VerticalAlignment="Bottom"
                                   Fill="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource ProcessControlForegroundBrush}}"
                                   Height="{Binding RectangleHeight, RelativeSource={RelativeSource AncestorType={x:Type pc:Tank}}, FallbackValue={StaticResource RectangleHeightFallback}}"/>
                    </Border>
                    <Path StrokeThickness="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource StrokeThicknessFallback}}"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource ProcessControlStrokeBrush}}"
                          Stretch="Fill"
                          Margin="-1">
                        <Path.Data>
                            <PathGeometry x:Name="GeoToBind"
                                          Figures="m 0,10 s 50,-10 100,0
                                                   l 0,80 s -50,10 -100,0 z"/>
                        </Path.Data>
                    </Path>
                </Grid>
            </DataTemplate>
        </Style.Resources>
    </Style>

    <!--Silo-Style-->
    <Style TargetType="pc:Silo" BasedOn="{StaticResource {x:Type pc:DefaultProcessControlBase}}">
        <Setter Property="Padding" Value="1"/>
        <Style.Resources>
            <DataTemplate x:Key="StyleTemplate">
                <Grid>
                    <Border Background="White">
                        <Border.OpacityMask>
                            <VisualBrush Stretch="Fill">
                                <VisualBrush.Visual>
                                    <Path Fill="White" Data="{Binding ElementName=GeoToBind}"/>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Border.OpacityMask>
                        <Rectangle VerticalAlignment="Bottom"
                                   Fill="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource ProcessControlForegroundBrush}}"
                                   Height="{Binding RectangleHeight, RelativeSource={RelativeSource AncestorType={x:Type pc:Tank}}, FallbackValue={StaticResource RectangleHeightFallback}}"/>
                    </Border>
                    <Path StrokeThickness="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource StrokeThicknessFallback}}"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type pc:DefaultProcessControlBase}}, FallbackValue={StaticResource ProcessControlStrokeBrush}}"
                          Stretch="Fill"
                          Margin="-1">
                        <Path.Data>
                            <PathGeometry x:Name="GeoToBind"
                                          Figures="m 0,0 l 30,-40 l 0,-62.5 l -60,0 l 0,62.5 z"/>
                        </Path.Data>
                    </Path>
                </Grid>
            </DataTemplate>
        </Style.Resources>
    </Style>

</ResourceDictionary>