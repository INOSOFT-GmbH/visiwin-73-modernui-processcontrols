<vw:View x:Class="HMI.PipeViewContext"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:vw="http://inosoft.com/visiwin7"
         xmlns:adapter="clr-namespace:HMI.Adapter"
         mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="{DynamicResource ApplicationBackgroundBrush}" Margin="10">

        <vw:GroupBox Header="@Views.ProcessControls.PipeVariableSettings" Foreground="{DynamicResource ForegroundDarkBrush}"
                  FontSize="{DynamicResource FontSize_Subheader}"
                  FontWeight="SemiBold">

            <vw:GroupBox.HeaderTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource Blue4Brush}"
                            CornerRadius="3"
                            Padding="10,5">
                        <vw:TextBlock LocalizableText="{Binding}"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource Blue5Brush}" />
                    </Border>
                </DataTemplate>
            </vw:GroupBox.HeaderTemplate>

            <Border Background="{DynamicResource Gray4Brush}"
                    CornerRadius="{DynamicResource CornerRadius}">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">

                        <vw:UpDownNumericVarIn UseVariableText="True" Width="300"
                                               VariableName="PipeOne{.ActualValue}"
                                               Margin="10" />

                        <vw:UpDownNumericVarIn UseVariableText="True" Width="300" VariableName="PipeOne{.SetValue}"
                                               Margin="10" />

                        <vw:UpDownNumericVarIn UseVariableText="True" Width="300" VariableName="PipeOne{.Errors}"
                                               Margin="10" />

                    </StackPanel>

                </Grid>

            </Border>

        </vw:GroupBox>

    </Grid>

</vw:View>