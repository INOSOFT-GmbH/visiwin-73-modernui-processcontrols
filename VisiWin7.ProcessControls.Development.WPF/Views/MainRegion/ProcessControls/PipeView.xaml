<vw:View x:Class="HMI.PipeView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:vw="http://inosoft.com/visiwin7"
         xmlns:pc="http://inosoft.com/ProcessControls"
         xmlns:adapter="clr-namespace:HMI.Adapter"
         mc:Ignorable="d"
         DataContext="{vw:AdapterBinding Type={x:Type adapter:ProcessControlAdapter}}"
         UseLayoutRounding="True" SnapsToDevicePixels="True"
         d:DesignWidth="1920" d:DesignHeight="880">

    <Grid x:Name="LayoutRoot" Background="{DynamicResource ApplicationBackgroundBrush}">

        <vw:ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="10"
                         Style="{StaticResource ScrollViewerDefaultStyle}">

            <StackPanel Orientation="Vertical">

                <vw:GroupBox Header="@Views.ProcessControls.PipeSystemExample"
                             Foreground="{DynamicResource ForegroundDarkBrush}"
                             FontSize="{DynamicResource FontSize_Subheader}"
                             FontWeight="SemiBold"
                             Margin="0,0,0,30">

                    <vw:GroupBox.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource Blue4Brush}"
                                    CornerRadius="3"
                                    Padding="10,5">
                                <vw:TextBlock LocalizableText="{Binding}"
                                              FontWeight="SemiBold"
                                              Foreground="{DynamicResource Blue5Brush}" />
                            </Border>
                        </DataTemplate>
                    </vw:GroupBox.HeaderTemplate>

                    <Border Background="{DynamicResource Gray4Brush}"
                            CornerRadius="{DynamicResource CornerRadius}"
                            Padding="0">

                        <Border Background="{DynamicResource GrayBrush}"
                                CornerRadius="{DynamicResource CornerRadius}"
                                Padding="30"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">

                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2"
                                                  Direction="315"
                                                  Color="Gray"
                                                  Opacity="0.3"
                                                  BlurRadius="8" />
                            </Border.Effect>

                            <Grid Width="354" Height="229">

                                <pc:Pipe Width="95" Height="11" HorizontalAlignment="Left" VerticalAlignment="Top"
                                         Margin="145,78,0,0"
                                         StructVariableName="PipeOne"
                                         StateBrushes="{StaticResource ProcessControlStates}"
                                         Mapping="{StaticResource PipeMapping}"
                                         Orientation="Right"
                                         Template="{StaticResource NotificationLessBaseControlTemplate}" />

                                <pc:Pipe Width="11" Height="121" HorizontalAlignment="Left" VerticalAlignment="Top"
                                         Margin="145,80,0,0"
                                         StructVariableName="PipeOne"
                                         StateBrushes="{StaticResource ProcessControlStates}"
                                         Mapping="{StaticResource PipeMapping}"
                                         Template="{StaticResource NotificationLessBaseControlTemplate}" />

                                <pc:Arrow Width="30" Height="26" HorizontalAlignment="Left" VerticalAlignment="Top"
                                          Margin="135,183,0,0"
                                          StructVariableName="PipeOne"
                                          StateBrushes="{StaticResource ProcessControlStates}"
                                          Mapping="{StaticResource ArrowMapping}" />

                                <pc:Pipe HorizontalAlignment="Left" VerticalAlignment="Top"
                                         Margin="253,19,0,0" Width="11"
                                         StructVariableName="PipeOne"
                                         StateBrushes="{StaticResource ProcessControlStates}"
                                         Mapping="{StaticResource PipeMapping}"
                                         Template="{StaticResource NotificationLessBaseControlTemplate}" d:LayoutOverrides="Height" />

                                <pc:PipeBridge Width="56" Height="41" HorizontalAlignment="Left"
                                               VerticalAlignment="Top"
                                               Margin="124,112,0,0"
                                               StrokeThickness="10"
                                               StructVariableName="PipeOne"
                                               StateBrushes="{StaticResource ProcessControlStates}"
                                               Template="{StaticResource NotificationLessBaseControlTemplate}"
                                               Mapping="{StaticResource PipeBridgeMapping}" />

                                <pc:Pipe Width="80" HorizontalAlignment="Left" VerticalAlignment="Top"
                                         Margin="276,78,0,0" Height="11"
                                         StructVariableName="PipeOne"
                                         StateBrushes="{StaticResource ProcessControlStates}"
                                         Mapping="{StaticResource PipeMapping}"
                                         Template="{StaticResource NotificationLessBaseControlTemplate}" />

                                <pc:Pipe Height="11" HorizontalAlignment="Left" VerticalAlignment="Top"
                                         Margin="174,133,0,0"
                                         StructVariableName="PipeOne"
                                         StateBrushes="{StaticResource ProcessControlStates}"
                                         Mapping="{StaticResource PipeMapping}"
                                         Template="{StaticResource NotificationLessBaseControlTemplate}" />

                                <pc:PipeConnection Width="16" HorizontalAlignment="Left" VerticalAlignment="Top"
                                                   Margin="250,130,0,0"
                                                   StructVariableName="PipeOne"
                                                   StateBrushes="{StaticResource ProcessControlStates}"
                                                   Mapping="{StaticResource PipeConnectionMapping}"
                                                   Template="{StaticResource NotificationLessBaseControlTemplate}"
                                                   Height="18" />

                                <pc:Pipe Width="120" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top"
                                         Margin="10,132,0,0"
                                         StructVariableName="PipeOne"
                                         StateBrushes="{StaticResource ProcessControlStates}"
                                         Template="{StaticResource NotificationLessBaseControlTemplate}"
                                         Mapping="{StaticResource PipeMapping}" />

                                <pc:PipeBridge Width="56" Height="41" HorizontalAlignment="Left"
                                               VerticalAlignment="Top"
                                               Margin="230,52,0,0"
                                               StrokeThickness="10"
                                               StructVariableName="PipeOne"
                                               StateBrushes="{StaticResource ProcessControlStates}"
                                               Template="{StaticResource NotificationLessBaseControlTemplate}"
                                               Mapping="{StaticResource PipeBridgeMapping}" d:LayoutOverrides="VerticalAlignment" />

                                <pc:Arrow Width="20" Height="42" HorizontalAlignment="Left" VerticalAlignment="Top"
                                          Margin="0,117,0,0"
                                          Orientation="Left"
                                          StructVariableName="PipeOne"
                                          StateBrushes="{StaticResource ProcessControlStates}"
                                          Mapping="{StaticResource ArrowMapping}" />

                                <pc:Arrow Height="47" HorizontalAlignment="Left" VerticalAlignment="Top"
                                          Margin="243,0,0,0" Width="31"
                                          Orientation="Bottom"
                                          StructVariableName="PipeOne"
                                          StateBrushes="{StaticResource ProcessControlStates}"
                                          Mapping="{StaticResource ArrowMapping}" />

                            </Grid>

                        </Border>

                    </Border>

                </vw:GroupBox>

            </StackPanel>

        </vw:ScrollViewer>

    </Grid>

</vw:View>